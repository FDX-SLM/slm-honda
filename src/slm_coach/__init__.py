"""SLM Sales Coach — fine-tuning & evaluation pipeline for a Vietnamese iPhone sales coach.

The package covers the full ``data -> train -> evaluate -> export`` pipeline and stops
at producing the model. Data *creation* and any serving layer are explicitly out of scope.
"""

__version__ = "0.1.0"
